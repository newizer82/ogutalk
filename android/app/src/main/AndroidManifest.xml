<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 카카오 OAuth 딥링크: com.ogutalk.app://login-callback#access_token=... -->
            <!-- host 제약 제거: Supabase가 fragment(#) 형태로 보낼 때 일부 OS에서 host 매칭 실패 회피 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.ogutalk.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- AdMob 앱 ID (필수 — 없으면 앱 시작 시 즉시 크래시) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1719848049915600~1950494616"/>

    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 알림 표시 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 정확한 시각 알람: Android 12~13 (자동 허용) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 정확한 시각 알람: Android 14+ (alarm clock 앱 - 자동 허용, 사용자 승인 불필요) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 방해 금지 모드 우회 -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- 진동 사용 (Haptics 플러그인 필수) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 재부팅 후 알람 자동 복원 (Capacitor LocalNotifications BootReceiver 동작 필수) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Doze Mode에서 AlarmManager 깨우기 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 배터리 최적화 면제 요청 (앱이 화이트리스트에 등록될 수 있도록) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

</manifest>
